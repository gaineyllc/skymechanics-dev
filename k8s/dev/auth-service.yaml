---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: skymechanics
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: skymechanics/auth-service:dev
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8200
        env:
        - name: PORT
          value: "8200"
        - name: FALKORDB_HOST
          value: "falkordb"
        - name: FALKORDB_PORT
          value: "6379"
        - name: FALKORDB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: skymechanics-secrets
              key: FALKORDB_PASSWORD
        - name: POSTGRES_HOST
          value: "skymechanics-postgres"
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: skymechanics-secrets
              key: POSTGRES_PASSWORD
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8200
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 8200
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: skymechanics
spec:
  selector:
    app: auth-service
  ports:
  - port: 8200
    targetPort: 8200
  type: ClusterIP
