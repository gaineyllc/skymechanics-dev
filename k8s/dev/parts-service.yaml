apiVersion: apps/v1
kind: Deployment
metadata:
  name: parts-service
  namespace: skymechanics
spec:
  replicas: 2
  selector:
    matchLabels:
      app: parts-service
  template:
    metadata:
      labels:
        app: parts-service
    spec:
      containers:
      - name: parts-service
        image: ghcr.io/gaineyllc/parts-service:latest
        ports:
        - containerPort: 8205
        env:
        - name: FALKORDB_HOST
          value: "falkordb"
        - name: FALKORDB_PORT
          value: "6379"
        - name: PORT
          value: "8205"
        - name: MULTI_TENANCY_ENABLED
          value: "true"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8205
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/health
            port: 8205
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: parts-service
  namespace: skymechanics
spec:
  selector:
    app: parts-service
  ports:
  - port: 8205
    targetPort: 8205
  type: ClusterIP
